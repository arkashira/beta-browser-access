# Browser Force

A Python project for reporting browser enforcement status and compliance.

## Usage

1. Install the project using `poetry install`.
2. Run the project using `python -m browser_force --os <os> --browser-version <browser-version> --enforcement-status <enforcement-status> --reason <reason>`.
3. The project will upload the enforcement status to a simulated API and rotate the local log.
